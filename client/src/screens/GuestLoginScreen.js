import { StyleSheet, Text, View } from "react-native";
import React from "react";

export const GuestLoginScreen = () => {
    return (
        <View>
            <Text>GuestLoginScreen</Text>
        </View>
    );
};

const styles = StyleSheet.create({});
